{
  "name": "rl-web",
  "version": "1.0.0",
  "description": "Rocket League Replay Analysis Online",
  "scripts": {
    "test": "start-server-and-test 1313 'cypress run'",
    "unit": "jest",
    "start": "npm run build && hugo serve",
    "build": "wasm-pack build --release crate && tsc && webpack --mode production && hugo"
  },
  "author": "",
  "license": "MIT",
  "jest": {
    "preset": "ts-jest"
  },
  "devDependencies": {
    "@types/jest": "^25.1.4",
    "assets-webpack-plugin": "^3.9.12",
    "cypress": "^4.2.0",
    "cypress-file-upload": "^4.0.4",
    "jest": "^25.1.0",
    "start-server-and-test": "^1.10.11",
    "ts-jest": "^25.2.1",
    "ts-loader": "^6.2.1",
    "typescript": "^3.8.3",
    "webpack": "^4.42.1",
    "webpack-cli": "^3.3.11",
    "worker-plugin": "^4.0.2"
  },
  "dependencies": {
    "preact": "^10.3.4",
    "sanitize.css": "^11.0.0"
  }
}
